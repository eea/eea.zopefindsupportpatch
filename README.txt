Introduction
============

This package contains a patch to the OFS.FindSupport module to prevent it from crashing when searching by unicode characters.

To install it, include it in the list of eggs and include its zcml.

